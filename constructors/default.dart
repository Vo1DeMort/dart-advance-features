//NOTE: Default constructor
//If you don't declare a constructor,
//Dart uses the default constructor.
//The default constructor is a generative constructor 
//without arguments or name.

class Point {
  // this will use generative constructor implicitly
  // WARNING: note that this accept 'null' value
  // unless these values are initialized like this
  //   double x = 0.0;
  //   double y = 0.0;
  double x;
  double y;
}
