// NOTE: there are "six" types of constructos in dart
// this is the follow along of officaial doc and exploration 

//NOTE: GENERATIVE constructor
//create new ->'instances'<- and initialize variables

class Point {
  // Instance variables to hold the coordinates of the point.
  double x;
  double y;

  // Generative constructor with initializing formal parameters:
  Point(this.x, this.y);
}
